import { View } from 'react-native';

export default function WhatsAppPlaceholder() {
  // This is a placeholder screen that will never be shown
  // The tab button is overridden to open WhatsApp directly
  return <View />;
}
